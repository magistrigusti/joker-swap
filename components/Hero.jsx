import React, { useState, useEffect } from "react";
import { IoCloeOutline } from "react-icons/io5";  

const Hero = ({ 
  setInputAmount,
  setLoader,
  setOpenToken,
  LOAD_TOKEN,
  token_1,
  token_2,
  setToken_1,
  setToken_2,
  swap,
}) => {
  return <div>Hero</div>;
};

export default Hero;
